# jax-omero-containers
Container image repository for JAX OMERO instances

## Local (on prem) build

The containers can be built locally using docker.

```bash
docker build omero-web -t omero-web
docker build omero-server -t omero-server
```
